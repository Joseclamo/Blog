<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.text_field :title, id: :post_title, placeholder: "Título de la entrada del Blog", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.text_field :author, id: :post_author,  placeholder: "Tu nombre aquí", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :blog_entry %>
    <%= form.text_area :blog_entry, id: :post_blog_entry, placeholder: "Cuéntanos algo...", class: "form-control" %>
  </div>

  <div class="form-group">
    <label for="@post">Selecciona una imagen</label>
      <input type="file" class="form-control-file" id="post.id">
  </div>

  <div class="actions">
    <%= form.submit "Enviar", class: "btn btn-primary" %>
  </div>
  <div>
    <br>
    <br>
  </div>
<% end %>
